name: Cypress Tests

on: deployment

env:
  CYPRESS_type: base

jobs:
  regression:
    runs-on: ubuntu-latest
    steps:
      - uses: joergmis/vercel-cypress@v0.0.1
        with:
          vercel-token: ${{ secrets.VERCEL_SECRET }}
          repo: website